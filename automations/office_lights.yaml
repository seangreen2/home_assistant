# Office Motion Lights
  - alias: Turn on office lights with motion
    trigger:
      platform: state
      entity_id: sensor.office_motion_motion_sensor
      to: 'on'
    condition:
      - condition: numeric_state
        entity_id: sensor.office_lux
        below: '6'
      - condition: state
        entity_id: group.family
        state: 'home'
      - condition: state
        entity_id: light.office
        state: 'off'
      - condition: numeric_state
        entity_id: sun.sun
        value_template: '{{ state.attributes.elevation }}'
        below: 90.0
        above: -6.0
    action:
      - service: light.turn_on
        data:
          entity_id: light.office
          brightness_pct: '100'
      - delay:
          seconds: '1'
      - service: light.turn_on
        data:
          entity_id: light.office
          brightness_pct: '100'

# Office Motion Lights 75
  - alias: Turn on office lights with motion 75
    trigger:
      platform: state
      entity_id: sensor.office_motion_motion_sensor
      to: 'on'
    condition:
      - condition: state
        entity_id: group.family
        state: 'home'
      - condition: state
        entity_id: light.office
        state: 'off'
      - condition: numeric_state
        entity_id: sun.sun
        value_template: '{{ state.attributes.elevation }}'
        below: -6.0
        above: -18.0
    action:
      - service: light.turn_on
        data:
          entity_id: light.office
          brightness_pct: '75'
      - delay:
          seconds: '1'
      - service: light.turn_on
        data:
          entity_id: light.office
          brightness_pct: '75'

# Office Motion Lights 75 transition
  - alias: Turn on office lights 75 transition
    trigger:
      - platform: state
        entity_id: sensor.office_motion_motion_sensor
        to: 'on'
      - platform: numeric_state
        entity_id: sun.sun
        value_template: '{{ state.attributes.elevation }}'
        below: -6.0
        above: -18.0
    condition:
      - condition: state
        entity_id: group.family
        state: 'home'
      - condition: state
        entity_id: light.office
        state: 'on'
      - condition: numeric_state
        entity_id: sun.sun
        value_template: '{{ state.attributes.elevation }}'
        below: -6.0
        above: -18.0
    action:
      service: light.turn_on
      data:
        entity_id: light.office
        brightness_pct: '75'
        transition: 30

# Office Motion Lights 65
  - alias: Turn on office lights with motion 65
    trigger:
      platform: state
      entity_id: sensor.office_motion_motion_sensor
      to: 'on'
    condition:
      - condition: state
        entity_id: group.family
        state: 'home'
      - condition: state
        entity_id: light.office
        state: 'off'
      - condition: numeric_state
        entity_id: sun.sun
        value_template: '{{ state.attributes.elevation }}'
        below: -18.0
    action:
      - service: light.turn_on
        data:
          entity_id: light.office
          brightness_pct: '60'
      - delay:
          seconds: '1'
      - service: light.turn_on
        data:
          entity_id: light.office
          brightness_pct: '60'

# Office Motion Lights 65 transition
  - alias: Turn on office lights 65 transition
    trigger:
      - platform: state
        entity_id: sensor.office_motion_motion_sensor
        to: 'on'
      - platform: numeric_state
        entity_id: sun.sun
        value_template: '{{ state.attributes.elevation }}'
        below: -18.0
    condition:
      - condition: state
        entity_id: group.family
        state: 'home'
      - condition: state
        entity_id: light.office
        state: 'on'
      - condition: numeric_state
        entity_id: sun.sun
        value_template: '{{ state.attributes.elevation }}'
        below: -18.0
    action:
      service: light.turn_on
      data:
        entity_id: light.office
        brightness_pct: '60'
        transition: 30

# Turn off Office Lights after one minute of no activity
  - alias: Turn office lights without motion one min
    trigger:
      platform: state
      entity_id: sensor.office_motion_motion_sensor
      to: 'off'
      for:
        minutes: '6'
    condition:
      condition: and
      conditions:
        - condition: numeric_state
          entity_id: sensor.laptop_socket
          below: '3'
        - condition: template
          value_template: "{{ not is_state('sensor.steam_76561197976250572', 'online') }}" # NOT condition
    action:
      service: light.turn_off
      data:
        entity_id: light.office
        transition: 2

# Turn off Office lights above lux
  - alias: Turn off office lights above lux
    trigger:
      platform: numeric_state
      entity_id: sensor.office_lux
      above: 31
    condition:
      - condition: time
        after: '06:00:00'
        before: '20:00:00'
    action:
      service: light.turn_off
      data:
        entity_id: light.office
        transition: 2
