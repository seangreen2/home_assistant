# Bathroom Lights on with Motion
- alias: Bathroom Lights on motion
  trigger:
    platform: state
    entity_id: binary_sensor.motion_sensor_158d000237a64a
    to: 'on'
  condition:
    condition: and
    conditions:
      - condition: state
        entity_id: group.family
        state: 'home'
      - condition: time
        after: '07:00:00'
        before: '21:30:00'
  action:
    - service: light.turn_on
      data:
        entity_id: light.bathroom
        brightness_pct: '100'
    - delay:
        seconds: '1'
    - service: light.turn_on
      data:
        entity_id: light.bathroom
        brightness_pct: '100'

# Bathroom Lights on with Motion at Bedtime
- alias: Bathroom Lights on motion bedtime
  trigger:
    platform: state
    entity_id: binary_sensor.motion_sensor_158d000237a64a
    to: 'on'
  condition:
    condition: and
    conditions:
      - condition: state
        entity_id: group.family
        state: 'home'
      - condition: time
        after: '21:30:00'
        before: '04:00:00'
  action:
    - service: light.turn_on
      data:
        entity_id: light.bathroom
        brightness_pct: '1'
    - delay:
        seconds: '1'
    - service: light.turn_on
      data:
        entity_id: light.bathroom
        brightness_pct: '1'

# Bathroom Lights on with Motion at Early Morning
- alias: Bathroom Lights on motion early morning
  trigger:
    platform: state
    entity_id: binary_sensor.motion_sensor_158d000237a64a
    to: 'on'
  condition:
    condition: and
    conditions:
      - condition: state
        entity_id: group.family
        state: 'home'
      - condition: state
        entity_id: light.bathroom
        state: 'off'
      - condition: time
        after: '04:00:00'
        before: '07:00:00'
  action:
    - service: light.turn_on
      data:
        entity_id: light.bathroom
        brightness_pct: '100'
        transition: 60
    - delay:
        seconds: '1'
    - service: light.turn_on
      data:
        entity_id: light.bathroom
        brightness_pct: '100'
        transition: 60

# Bathroom Lights dim from full in bed
- alias: Bathroom Lights Dim in Bed
  trigger:
    - platform: state
      entity_id: input_boolean.sean
      to: 'on'
    - platform: state
      entity_id: input_boolean.emily
      to: 'on'
  condition:
    - condition: time
      after: '19:00:00'
      before: '23:00:00'
    - condition: state
      entity_id: light.bathroom
      state: 'on'
    - condition: or
      conditions:
        - condition: state
          entity_id: input_boolean.sean
          state: 'on'
        - condition: state
          entity_id: input_boolean.emily
          state: 'on'
  action:
    - service: light.turn_on
      data:
        entity_id: light.bathroom
        brightness_pct: '1'
        transition: 3
    - delay:
        seconds: '1'
    - service: light.turn_on
      data:
        entity_id: light.bathroom
        brightness_pct: '1'
        transition: 3

# Bathroom Lights off without Motion night
- alias: Bathroom Lights off no motion night
  trigger:
    platform: state
    entity_id: binary_sensor.motion_sensor_158d000237a64a
    to: 'off'
    for:
      seconds: 30
  condition:
    - condition: time
      after: '21:00:01'
      before: '04:20:00'
  action:
    service: light.turn_off
    data:
      entity_id: light.bathroom
      transition: 2

# Bathroom Lights off without Motion
- alias: Bathroom Lights off no motion
  trigger:
    platform: state
    entity_id: binary_sensor.motion_sensor_158d000237a64a
    to: 'off'
    for:
      minutes: 3
  condition:
    - condition: time
      after: '04:20:01'
      before: '21:00:00'
  action:
    service: light.turn_off
    data:
      entity_id: light.bathroom
      transition: 2

# Detect if someone is showering and disables lights off automations
- alias: Showering
  trigger:
    platform: numeric_state
    entity_id: sensor.humidity_158d0002478a21
    above: '66'
  action:
    - service: automation.turn_off
      entity_id: automation.bathroom_lights_off_no_motion
    - service: input_boolean.turn_on
      data:
        entity_id: input_boolean.showering

# Detect if someone is not showering and enables lights off automations
- alias: Not Showering
  trigger:
    platform: numeric_state
    entity_id: sensor.humidity_158d0002478a21
    below: '66'
  action:
    - service: automation.turn_on
      entity_id: automation.bathroom_lights_off_no_motion
    - service: input_boolean.turn_off
      data:
        entity_id: input_boolean.showering

# Turn on air fresh for one minute each hour when home
- alias: Turn on air fresh
  trigger:
    platform: time
    hours: '/1'
    minutes: '00'
    seconds: '00'
  condition:
    condition: and
    conditions:
      - condition: state
        entity_id: group.family
        state: 'home'
      - condition: time
        after: '06:00:00'
        before: '22:15:00'
  action:
    - service: switch.turn_on
      entity_id:  switch.smart_plug_one
    - delay:
        minutes: '10'
    - service: switch.turn_off
      entity_id:  switch.smart_plug_one
